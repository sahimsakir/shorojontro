import { pgTable, text, integer, bigint, index, primaryKey } from 'drizzle-orm/pg-core';
export const rooms=pgTable('rooms',{code:text('code').primaryKey(),name:text('name').notNull(),private:integer('private').notNull(),password:text('password'),state:text('state').notNull(),revision:integer('revision').notNull().default(0),lastActive:bigint('last_active',{mode:'number'}),updated:bigint('updated',{mode:'number'}).notNull()},t=>[index('idx_rooms_public_updated').on(t.private,t.updated),index('idx_rooms_last_active').on(t.lastActive)]);
export const sessions=pgTable('sessions',{id:text('id').primaryKey(),name:text('name').notNull(),created:bigint('created',{mode:'number'}).notNull(),attempts:integer('attempts').notNull().default(0),window:bigint('window_start',{mode:'number'}).notNull().default(0)});
export const accounts=pgTable('accounts',{username:text('username').primaryKey(),playerId:text('player_id').notNull().unique().references(()=>sessions.id),passwordHash:text('password_hash').notNull(),salt:text('salt').notNull(),created:bigint('created',{mode:'number'}).notNull()});
export const loginTokens=pgTable('login_tokens',{tokenHash:text('token_hash').primaryKey(),playerId:text('player_id').notNull().references(()=>sessions.id),expires:bigint('expires',{mode:'number'}).notNull()},t=>[index('idx_login_tokens_player').on(t.playerId),index('idx_login_tokens_expiry').on(t.expires)]);
export const authLimits=pgTable('auth_limits',{key:text('key').primaryKey(),attempts:integer('attempts').notNull(),windowStart:bigint('window_start',{mode:'number'}).notNull()});
export const playerResults=pgTable('player_results',{roundId:text('round_id').notNull(),playerId:text('player_id').notNull(),roomCode:text('room_code').notNull(),won:integer('won').notNull(),bluffs:integer('bluffs').notNull(),challenges:integer('challenges').notNull(),challengeWins:integer('challenge_wins').notNull(),finished:bigint('finished',{mode:'number'}).notNull()},t=>[primaryKey({columns:[t.roundId,t.playerId]}),index('idx_player_results_player').on(t.playerId)]);
export const maintenance=pgTable('maintenance',{name:text('name').primaryKey(),lastRun:bigint('last_run',{mode:'number'}).notNull().default(0)});
